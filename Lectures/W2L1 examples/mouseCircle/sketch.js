function setup() {
    createCanvas(400, 300);
}

function draw() {
    background(0);
    // mouseX and mouseY are variables that store the current coordinates of the mouse
    circle(mouseX, mouseY, 40);
}
